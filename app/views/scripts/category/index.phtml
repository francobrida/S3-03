<!-- Contenedor principal centrado -->
<div class="max-w-4xl mx-auto p-6">
    
    <!-- LA PASTILLA GRANDE -->
    <div class="bg-white rounded-3xl overflow-hidden shadow-lg mt-10">
        
        <!-- PARTE VERDE (arriba) -->
        <div class="bg-teal-700 text-white p-8">
            
            <h1 class="text-4xl font-bold mb-2">
                Gestor de categorías
            </h1>
            
            <p class="opacity-80 mb-6">
                <?= date('l, d F'); ?>
            </p>

            <!-- FILTRO - FRAN -->
            <div class="filtersContainer">
                <form action="<?= WEB_ROOT ?>/category/filter" method="GET" style="display: flex; gap: 10px; align-items: center;">
                        
                    <input type="text" name="search" placeholder="Buscar por nombre..." 
                        value="<?= $_GET['search'] ?? '' ?>"
                        style="color: black; padding: 5px 10px; border-radius: 8px; border: 1px solid #ccc;">

                    <button type="submit">Buscar</button>
                </form>
            </div>

            <!-- FORMULARIO HORIZONTAL -->
            <form method="POST" action="<?= WEB_ROOT ?>/category/add" class="flex gap-4 items-end">
                
                <!-- Campo NOMBRE -->
                <div class="flex-1">
                    <label for="name" class="block text-white font-semibold mb-2">
                        Nombre:
                    </label>
                    <input type="text" 
                           id="name" 
                           name="name" 
                           class="w-full border-2 border-gray-400 rounded-xl px-4 py-3 shadow-md text-gray-600"
                           required>
                </div>
                
                <!-- Campo DESCRIPCIÓN -->
                <div class="flex-1">
                    <label for="description" class="block text-white font-semibold mb-2">
                        Descripción:
                    </label>
                    <input type="text" 
                           id="description" 
                           name="description" 
                           class="w-full border-2 border-gray-400 rounded-xl px-4 py-3 shadow-md text-gray-600"
                           required>
                </div>
                
                <!-- Botón AÑADIR -->
                <button type="submit" 
                        class="bg-teal-700 text-white px-6 py-3 rounded-xl font-semibold hover:opacity-90 transition-opacity border-2 border-white shadow-md">
                    ➕ Añadir
                </button>
                
            </form>
            
        </div>
        
        <!-- PARTE BLANCA (abajo) - LISTA DE CATEGORÍAS -->
        <div class="bg-white p-8">
            
            <?php foreach ($this->categories as $category): ?>
                
                <!-- Mini-pastilla de cada categoría -->
                <div class="bg-white rounded-2xl p-5 mb-4 flex items-center justify-between shadow-md border-2 border-gray-400">
                    
                    <!-- Contenido: nombre y descripción -->
                    <div>
                        <h3 class="text-xl font-bold text-teal-700">
                            <?= htmlspecialchars($category['name']); ?>
                        </h3>
                        <p class="text-gray-600 mt-1">
                            <?= htmlspecialchars($category['description']); ?>
                        </p>
                    </div>
                    
                    <!-- Botones: editar y eliminar -->
                    <div class="flex gap-3">
                        
                        <!-- Botón editar -->
                        <a href="<?= WEB_ROOT ?>/category/edit?id=<?= $category['id'] ?>" 
                           class="text-2xl hover:scale-110 transition-transform">
                            ✏️
                        </a>
                        
                        <!-- Botón eliminar -->
                        <a href="<?= WEB_ROOT ?>/category/delete?id=<?= $category['id'] ?>" 
                           class="text-2xl hover:scale-110 transition-transform"
                           onclick="return confirm('¿Seguro que quieres eliminar esta categoría?')">
                            🗑️
                        </a>
                        
                    </div>
                    
                </div>
                
            <?php endforeach; ?>
            
        </div>
        
    </div>
    
</div>