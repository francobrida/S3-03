<div class="container mx-auto p-4">

    <?php if (!isset($_SESSION['user_id'])): ?>
        
        <div class="flex flex-col items-center justify-center min-h-[60vh] space-y-8">
            <h1 class="text-3xl font-bold text-gray-800">Welcome to Task Manager</h1>

            <?php if (isset($_SESSION['error'])): ?>
                <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative w-full max-w-2xl">
                    <?= $_SESSION['error']; unset($_SESSION['error']); ?>
                </div>
            <?php endif; ?>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-12 w-full max-w-5xl">
                
                <div class="bg-white p-6 rounded shadow-md border-t-4 border-blue-500">
                    <h2 class="text-xl font-bold mb-4">Login</h2>
                    <form action="<?= WEB_ROOT; ?>/login" method="POST" class="flex flex-col space-y-3">
                        <input type="text" name="nickname" placeholder="Nickname" required class="border p-2 rounded">
                        <input type="password" name="password" placeholder="Password" required class="border p-2 rounded">
                        <button type="submit" class="bg-blue-500 text-white py-2 rounded hover:bg-blue-600 transition">Enter</button>
                    </form>
                </div>

                <div class="bg-white p-6 rounded shadow-md border-t-4 border-green-500">
                    <h2 class="text-xl font-bold mb-4">Register</h2>
                    <form action="<?= WEB_ROOT; ?>/register" method="POST" class="flex flex-col space-y-3">
                        <div class="grid grid-cols-2 gap-2">
                            <input type="text" name="name" placeholder="Name" required class="border p-2 rounded">
                            <input type="text" name="surname" placeholder="Surname" required class="border p-2 rounded">
                        </div>
                        <input type="text" name="nickname" placeholder="Nickname" required class="border p-2 rounded">
                        <input type="email" name="email" placeholder="Email" required class="border p-2 rounded">
                        <input type="password" name="password" placeholder="Password" required class="border p-2 rounded">
                        <button type="submit" class="bg-green-500 text-white py-2 rounded hover:bg-green-600 transition">Sign Up</button>
                    </form>
                </div>
            </div>
        </div>

    <?php else: ?>

        <div class="flex justify-between items-center bg-gray-50 p-4 border-b">
             <h1 class="text-xl font-semibold">User Management System</h1>
             <div class="flex items-center gap-4">
                 <span class="text-sm text-gray-600">Hi, <strong><?= $_SESSION['nickname']; ?></strong></span>
                 <a href="<?= WEB_ROOT; ?>/logout" class="bg-red-500 text-white px-3 py-1 rounded text-sm hover:bg-red-600">Logout</a>
             </div>
        </div>

        <div class="mt-6">
            <?php 
                
            ?>
        </div>

    <?php endif; ?>

</div>
