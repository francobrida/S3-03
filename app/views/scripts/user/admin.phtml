<?php include 'header.phtml'; ?>

<div>
    <div class="filtersContainer">
                <form action="<?= WEB_ROOT ?>/user/filter" method="GET" style="display: flex; gap: 10px; align-items: center;">
                        
                    <input type="text" name="search" placeholder="Buscar por nickname..." 
                        value="<?= $_GET['search'] ?? '' ?>"
                        style="color: black; padding: 5px 10px; border-radius: 8px; border: 1px solid #ccc;">

                    <button type="submit">Buscar</button>
                </form>
    </div>
    <a href="<?php echo WEB_ROOT; ?>/userAddView">Agregar nuevo usuario</a>
</div>

<table border="1" width="100%">
    <thead>
        <tr>
            <th>Nickname</th>
            <th>Nombre</th>
            <th>Apellido</th>
            <th>Email</th>
            <th>Tipo</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        <?php 
        $users = isset($this->_data['users']) ? $this->_data['users'] : []; 
        ?>
        <?php foreach ($users as $user): ?>
            <tr>
                <td><?php echo isset($user['nickname']) ? $user['nickname'] : ''; ?></td>
                <td><?php echo isset($user['name']) ? $user['name'] : ''; ?></td>
                <td><?php echo isset($user['surname']) ? $user['surname'] : ''; ?></td>
                <td><?php echo isset($user['email']) ? $user['email'] : ''; ?></td>
                <td><?php echo isset($user['type']) ? $user['type'] : ''; ?></td>
                <td>
                    <?php 
                    $userId = isset($user['id']) ? $user['id'] : ''; 
                    $urlEdit = WEB_ROOT . "/user/edit?id=" . $userId;
                    ?>
                    <a href="<?php echo $urlEdit; ?>" 
                        <button type="button">Editar</button>
                    </a>

                    <?php 
                    $userId = isset($user['id']) ? $user['id'] : ''; 
                    $urlDelete = WEB_ROOT . "/user/delete?id=" . $userId;
                    ?>
                    <a href="<?php echo $urlDelete; ?>" 
                       onclick="return confirm('Confirmar eliminar usuario con sus respectivas tareas');">
                        <button type="button">Eliminar</button>
                    </a>
                </td>
            </tr>
        <?php endforeach; ?>
    </tbody>
</table>