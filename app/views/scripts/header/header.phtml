<?php $currentUri = $_SERVER['REQUEST_URI']; ?>

<style>
    
    .nav-link {
        position: relative;
        padding: 4px 0;
    }
    .nav-link.active::after {
        content: '';
        position: absolute;
        bottom: -2px; 
        left: 0;
        width: 100%;
        height: 1px;
        background-color: white;
        border-radius: 2px;
    }
</style>

<div class="flex items-center w-full px-12 py-6 bg-teal-800 text-white">

    <div class="flex-1"></div>

    <nav class="flex items-center gap-12">
        <a href="<?= WEB_ROOT ?>/task" 
           class="nav-link font-medium text-base transition-opacity <?= (strpos($currentUri, 'task') !== false) ? 'active' : 'opacity-70 hover:opacity-100' ?>">
           Gestor de tareas
        </a>

        <?php if (isset($_SESSION['type']) && $_SESSION['type'] === 'Admin'): ?>
            <a href="<?= WEB_ROOT ?>/category" 
               class="nav-link font-medium text-base transition-opacity <?= (strpos($currentUri, 'category') !== false) ? 'active' : 'opacity-70 hover:opacity-100' ?>">
               Gestor de categorías
            </a>

            <a href="<?= WEB_ROOT ?>/userAdmin" 
               class="nav-link font-medium text-base transition-opacity <?= (strpos($currentUri, 'userAdmin') !== false || strpos($currentUri, 'user/') !== false) ? 'active' : 'opacity-70 hover:opacity-100' ?>">
               Gestor de usuarios
            </a>
        <?php endif; ?>
    </nav>

    <div class="flex-1 flex justify-end items-center gap-6">
        <span class="text-teal-300 italic font-medium">
            @<?= $_SESSION['nickname'] ?? 'usuario' ?>
        </span>

        <?php if (isset($_SESSION['user_id'])): ?>
            <a href="<?= WEB_ROOT ?>/logout" 
               class="text-sm bg-white/10 hover:bg-white/20 px-4 py-1.5 rounded-lg border border-white/20 transition">
               LOG OUT
            </a>
        <?php endif; ?>
    </div>

</div>